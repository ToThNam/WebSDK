export interface Options_AudioSpeakerDetector {
    uri?: string;
    logger?: any; 
    audioContext?: any; 
}
export interface Meter_GetAudioVolumeMeters {
    onValue: (arg0: () => void) => void; 
}
export interface Options_SetupAudioVolumeMeter {
     alpha: any; 
}
export interface Options_SetupSpeakingDetection {
    alpha: any; 
}